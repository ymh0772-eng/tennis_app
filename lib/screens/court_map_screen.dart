import 'package:flutter/material.dart';

class CourtMapScreen extends StatelessWidget {
  const CourtMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('코트 현황')),
      body: const Center(child: Text('준비 중입니다')),
    );
  }
}
